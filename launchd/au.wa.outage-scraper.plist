<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>au.wa.outage-scraper</string>

    <!-- Run run.sh on an interval. Edit the path if you move the project. -->
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/path/to/lights-out/run.sh</string>
    </array>

    <!-- Every 600 seconds (10 minutes) while the Mac is awake/logged in. -->
    <key>StartInterval</key>
    <integer>600</integer>

    <!-- Catch up with one run shortly after load. -->
    <key>RunAtLoad</key>
    <true/>

    <key>WorkingDirectory</key>
    <string>/path/to/lights-out</string>

    <key>StandardOutPath</key>
    <string>/path/to/lights-out/scraper.log</string>
    <key>StandardErrorPath</key>
    <string>/path/to/lights-out/scraper.log</string>
</dict>
</plist>
