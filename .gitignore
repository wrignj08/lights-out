# Reconstructed artefacts are derived from git history; rebuild on demand.
data/events.csv
data/events.geojson

# runtime log from run.sh / launchd
scraper.log

# macOS / Python noise
.DS_Store
__pycache__/
*.pyc
